---
- name: Set NetworkManager
  block:
  - name: Copy NetworkManager configuration file
    copy: 
      src: conf.d/
      dest: /etc/NetworkManager/conf.d
 
  - name: Restart NetworkManager
    systemd:
      name: NetworkManager
      state: restarted
      enabled: yes

- name: Replace resolv.conf
  template: 
    src: redhat.resolv.conf.j2 
    dest: /etc/resolv.conf
